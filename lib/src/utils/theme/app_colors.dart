import 'package:flutter/material.dart';

final class AppColors {
  //light mode colors
  static const light = {
    'primary': Colors.yellow,
  };

  //dark mode colors
  static const dark = {
    'primary': Color.fromARGB(255, 6, 23, 49),
  };
}
